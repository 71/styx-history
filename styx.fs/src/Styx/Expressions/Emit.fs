namespace Styx.Expressions

open Styx

// type Emit(ty, span) =
//     inherit Expr(span)

//     override __.State = (ty :> IBindable).State

//     override __.Compile(_) = ()

//     override __.ToString() = "emit"
